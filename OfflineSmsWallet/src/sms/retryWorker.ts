// Placeholder for WorkManager scheduling via Headless JS or native Worker
export function scheduleRetry(txid: string) {
  // In full implementation, enqueue WorkManager job to retry sending
  // This placeholder keeps the TypeScript contract
}

